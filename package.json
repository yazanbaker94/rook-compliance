{
  "name": "rook-compliance",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "dev:api": "npm --prefix services/api run dev",
    "dev:mobile": "npm --prefix apps/mobile start",
    "test:api": "npm --prefix services/api test",
    "test:ai": "cd services/ai && python -m pytest -q",
    "verify:web": "npm --prefix apps/web run lint && npm --prefix apps/web run build",
    "verify:api": "npm --prefix services/api run prisma:generate && npm --prefix services/api run build && npm --prefix services/api test",
    "verify:mobile": "npm --prefix apps/mobile run typecheck",
    "verify": "npm run verify:web && npm run verify:api && npm run verify:mobile && npm run test:ai"
  }
}
